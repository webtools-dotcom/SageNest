#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

node scripts/sync-readme-worktree.mjs

if ! git diff --quiet -- README.md; then
  git add README.md
  echo "README.md worktree structure re-staged after sync."
fi
